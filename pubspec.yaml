name: is_app
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.1.6
  equatable: ^1.0.0
  flutter_bloc: ^3.2.0
  json_serializable: ^3.3.0
  cupertino_icons: ^0.1.2
  flutter_screenutil: ^1.1.0
  image_picker: ^0.6.1+10

dev_dependencies:
  flutter_test:
    sdk: flutter
  json_serializable: ^3.3.0
  build_runner: ^1.8.1
  test:
  mockito:
  bloc_test: ^3.0.1

flutter:


  uses-material-design: true

  assets:
    - assets/ISBikesData.json

